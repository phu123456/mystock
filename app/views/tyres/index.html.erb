<p id="notice"><%= notice %></p>

<h1>Tyres</h1>

<table>
  <thead>
    <tr>
      <th>Serial</th>
      <th>Brand</th>
      <th>Truck</th>
      <th>Position</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tyres.each do |tyre| %>
      <tr>
        <td><%= tyre.serial %></td>
        <td><%= tyre.brand %></td>
        <td><%= tyre.truck_id %></td>
        <td><%= tyre.position %></td>
        <td><%= tyre.status %></td>
        <td><%= link_to 'Show', tyre %></td>
        <td><%= link_to 'Edit', edit_tyre_path(tyre) %></td>
        <td><%= link_to 'Destroy', tyre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tyre', new_tyre_path %>
